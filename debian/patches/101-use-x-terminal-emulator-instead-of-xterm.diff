Description: use x-terminal-emulator instead of xterm
 x-terminal-emulator is a sensible default terminal emulator on Debian, so
 use it instead of xterm in the default configuration.
Forwarded: not-needed
Author: Andrea Bolognani <eof@kiyuko.org>
Last-Update: 2009-09-23
Index: scrotwm.git/scrotwm.conf
===================================================================
--- scrotwm.git.orig/scrotwm.conf	2010-07-21 17:41:14.935699577 +0200
+++ scrotwm.git/scrotwm.conf	2010-07-21 17:41:22.047701129 +0200
@@ -19,13 +19,13 @@
 title_class_enabled	= 0
 
 # spawn app
-# program[term]		= xterm
+# program[term]		= x-terminal-emulator
 # program[screenshot_all]	= screenshot.sh full
 # program[screenshot_wind]	= screenshot.sh window
 # program[lock]		= xlock
 # program[initscr]	= initscreen.sh
 # program[menu]		= dmenu_run -fn $bar_font -nb $bar_color -nf $bar_font_color -sb $bar_border -sf $bar_color
-spawn_term		= xterm
+spawn_term		= x-terminal-emulator
 
 # dialog box size ratio .3 >= r < 1
 dialog_ratio		= 0.6
Index: scrotwm.git/scrotwm.c
===================================================================
--- scrotwm.git.orig/scrotwm.c	2010-07-21 17:41:14.935699577 +0200
+++ scrotwm.git/scrotwm.c	2010-07-21 17:41:22.051701658 +0200
@@ -2860,7 +2860,7 @@
 void
 setup_spawn(void)
 {
-	setconfspawn("term",		"xterm",		0);
+	setconfspawn("term",		"x-terminal-emulator",		0);
 	setconfspawn("screenshot_all",	"screenshot.sh full",	0);
 	setconfspawn("screenshot_wind",	"screenshot.sh window",	0);
 	setconfspawn("lock",		"xlock",		0);
@@ -4574,7 +4574,7 @@
 	if ((bar_fonts[2] = strdup("-misc-fixed-medium-r-*-*-*-*-*-*-*-*-*-*"))
 	    == NULL)
 		err(1, "setup_globals: strdup");
-	if ((spawn_term[0] = strdup("xterm")) == NULL)
+	if ((spawn_term[0] = strdup("x-terminal-emulator")) == NULL)
 		err(1, "setup_globals: strdup");
 	if ((clock_format = strdup("%a %b %d %R %Z %Y")) == NULL)
 		errx(1, "strdup");
