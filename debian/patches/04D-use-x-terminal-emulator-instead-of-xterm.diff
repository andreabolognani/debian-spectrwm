x-terminal-emulator is a sensible default terminal emulator on Debian, so use
it instead of xterm in the default configuration.
Index: scrotwm/scrotwm.conf
===================================================================
--- scrotwm.orig/scrotwm.conf	2009-09-14 12:17:53.000000000 +0200
+++ scrotwm/scrotwm.conf	2009-09-14 12:29:15.000000000 +0200
@@ -17,13 +17,13 @@
 title_class_enabled	= 0
 
 # spawn app
-# program[term]		= xterm
+# program[term]		= x-terminal-emulator
 # program[screenshot_all]	= screenshot.sh full
 # program[screenshot_wind]	= screenshot.sh window
 # program[lock]		= xlock
 # program[initscr]	= initscreen.sh
 # program[menu]		= dmenu_run -fn $bar_font -nb $bar_color -nf $bar_font_color -sb $bar_border -sf $bar_color
-spawn_term		= xterm
+spawn_term		= x-terminal-emulator
 
 # dialog box size ratio .3 >= r < 1
 dialog_ratio		= 0.6
